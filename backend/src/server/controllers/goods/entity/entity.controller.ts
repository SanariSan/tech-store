import type { NextFunction, Response } from 'express';
import type { TRequestNarrowed } from '../../../express.type';

export const entityCTR = (req: TRequestNarrowed, res: Response, next: NextFunction) => {
  // FOR DETAILED INFO ON PRODUCT
  // NOT IN USE NOW

  console.log('123');

  return;
};
